// Escreva uma função que receba dois valores, digamos a e b, como argumentos
// Retorna true se os dois valores forem iguais e do mesmo tipo


const igualdadeEstrita = (a, b, res) => {
    return (a === b ? res = true : res = false)
};
